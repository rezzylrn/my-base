"use client";
import Link from "next/link";
import { usePathname } from "next/navigation";

export default function Navbar() {
  const pathname = usePathname();
  return (
    <nav className="bg-gray-900 border-b border-gray-800 px-6 py-3 flex items-center justify-between">
      <div className="flex items-center gap-8">
        <Link href="/dashboard" className="text-xl font-bold text-cyan-400">
          ☠️ Nemesis
        </Link>
        <div className="flex gap-4">
          <Link
            href="/dashboard"
            className={`text-sm ${pathname === "/dashboard" ? "text-cyan-300" : "text-gray-400"} hover:text-white`}
          >
            Victims
          </Link>
          <Link
            href="/dashboard/generate-apk"
            className={`text-sm ${pathname === "/dashboard/generate-apk" ? "text-cyan-300" : "text-gray-400"} hover:text-white`}
          >
            Generate APK
          </Link>
        </div>
      </div>
      <div className="text-gray-500 text-sm">ShadowNet v1.0</div>
    </nav>
  );
}
